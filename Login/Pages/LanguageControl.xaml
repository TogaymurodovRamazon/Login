<UserControl x:Class="Login.Pages.LanguageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Login.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Snow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <Label Content="Language" FontSize="20"/>

        </StackPanel>
        <Grid Grid.Row="0" VerticalAlignment="Bottom" Height="3"
              Background="WhiteSmoke">
            
        </Grid>

        <StackPanel Grid.Row="1" Width="300" Orientation="Horizontal"
                    Height="30" VerticalAlignment="Top" Margin="0 20 450 0">
            <Label Content="Select Language" Margin="0 0 20 0" FontSize="18"/>
            <RadioButton IsChecked="True" Content="UZ" Margin="0 0 20 0"
                         VerticalAlignment="Center"/>
            <RadioButton Content="EN" VerticalAlignment="Center"/>

        </StackPanel>
    </Grid>
</UserControl>
